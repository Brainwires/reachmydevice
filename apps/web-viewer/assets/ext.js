// Deployment extension point (see window.rmdExt in index.html).
// The open build ships this as an empty stub so the optional /app/ext.js load
// never 404s on self-host. A hosted deployment overlays its own ext.js here to
// register additional console screens/actions.
