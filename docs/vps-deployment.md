# VPS deployment (Phase 5 — placeholder)

The rendezvous unit (axum + SQLite + coturn) ships as one docker-compose, sized for 1 vCPU / 1 GB.
Will document: TLS via ACME/Let's Encrypt, coturn hardening, rate limiting, firewall/ports, backups.
