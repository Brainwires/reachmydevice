# Automatic HTTPS via Let's Encrypt for the rendezvous API + WebSocket.
# Set RMD_DOMAIN in .env; Caddy provisions and renews the certificate.
{$RMD_DOMAIN} {
	encode gzip
	# WebSocket upgrade + REST both proxy to the rendezvous service.
	reverse_proxy rendezvous:8080
}
