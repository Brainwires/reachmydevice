# Rust
/target
**/*.rs.bk
*.pdb

# Local secrets / env
.env
*.local

# ReachMyDevice runtime artifacts
/debug-bundles
*.log

# Release build output + signing material (NEVER commit the secret key)
/dist
*.minisig
minisign.key

# OS cruft
.DS_Store
